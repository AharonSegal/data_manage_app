/** Project B — Overview page stub. Content will be added in Stage 2. */
import { PageHeader } from '@/shared/components/Layout/PageHeader';

const OverviewPage = () => {
  return (
    <div className="p-6">
      <PageHeader
        title="Project B — Overview"
        subtitle="Project status, KPIs, and recent activity"
      />
      {/* No content yet — Stage 2 */}
    </div>
  );
};

export default OverviewPage;
