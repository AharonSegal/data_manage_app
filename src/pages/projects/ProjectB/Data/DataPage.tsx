import { PageHeader } from '@/shared/components/Layout/PageHeader';

const DataPage = () => {
  return (
    <div className="p-6">
      <PageHeader
        title="Project B — Data"
        subtitle="Manage project records and data"
      />
      {/* No content yet — Stage 2 */}
    </div>
  );
};

export default DataPage;
