/** Project B — Actions page stub. Content (email/WhatsApp workflows) will be added in Stage 3. */
import { PageHeader } from '@/shared/components/Layout/PageHeader';

const ActionsPage = () => {
  return (
    <div className="p-6">
      <PageHeader
        title="Project B — Actions"
        subtitle="Automated workflows and approval processes"
      />
      {/* No content yet — Stage 3 */}
    </div>
  );
};

export default ActionsPage;
