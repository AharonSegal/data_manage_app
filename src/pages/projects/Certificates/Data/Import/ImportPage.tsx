/** Certificates — Import page stub. Content will be added in Stage 2. */
import { PageHeader } from '@/shared/components/Layout/PageHeader';

const ImportPage = () => {
  return (
    <div className="p-6">
      <PageHeader
        title="Import Data"
        subtitle="Upload and import certificate records from files"
      />
      {/* No content yet — Stage 2 */}
    </div>
  );
};

export default ImportPage;
