import { PageHeader } from '@/shared/components/Layout/PageHeader';

const DataPage = () => {
  return (
    <div className="p-6">
      <PageHeader
        title="Certificates — Data"
        subtitle="Manage certificate records and data imports"
      />
      {/* No content yet — Stage 2 */}
    </div>
  );
};

export default DataPage;
